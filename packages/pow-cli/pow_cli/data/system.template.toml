[asset]
# Flag indicating if a local asset folder is currently attached.
# Managed by 'pow asset attach' and 'pow asset detach'.
use_local_asset = false

# The absolute path to the currently attached local asset directory.
# This will be populated when an asset folder is attached.
local_asset_path = ""
